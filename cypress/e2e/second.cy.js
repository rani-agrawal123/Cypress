
describe('Test Suit-collection of tests', () => {
    it('Single Tests', () => {
        
        cy.visit("https://students.masaischool.com/")

    });
});
// one describe block can have multiple iit blocks
// npx cypress run (node_module/.bin/cypress run) -this will execute all specs in the given folder